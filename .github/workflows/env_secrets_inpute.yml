name: demo secrets env
on:
    workflow_dispatch:
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Echo
              run: echo "Hello World from second CI"

            - name: Insatll
              env:
                  APP_ENV: DEMO
              run: echo "Environment $APP_ENV"
